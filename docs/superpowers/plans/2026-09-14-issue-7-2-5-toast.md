# フラッシュメッセージ（トースト）の共通部品 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 画面遷移を伴わない操作の結果を伝えるトーストの共通部品を、依存パッケージを足さずに作る。7-3 が `toast.success("下書きを保存しました")` の 1 行で使える状態にする。

**Architecture:** トーストの一覧はモジュールスコープのストア（`toast-store.ts`）が持つ。React を知らないので `api.ts` のような React の外からも呼べ、キュー管理を純粋ロジックとして Vitest で検証できる。ビューポート（`toast-viewport.tsx`）が `useSyncExternalStore` で購読し、ルートレイアウトに 1 つだけ置かれて描画する。既存の `tokenStore` / `auth-context.tsx` と同じ構造。

**Tech Stack:** Next.js 16（App Router）／ React 19 ／ TypeScript ／ TailwindCSS v4 ／ Vitest。**新しい依存は追加しない。**

**Spec:** `docs/superpowers/specs/2026-09-14-issue-7-2-5-toast-design.md`

**Issue:** GitHub #104（`docs/issues_backlog.md` 7-2.5）

**Branch:** `feat/toast-flash-message`（既に作成済み。設計書のコミット `2942076` が載っている）

## Global Constraints

このプロジェクト全体の規約。**全タスクの要件に暗黙に含まれる。**

- **依存パッケージを追加しない。** `frontend/package.json` は変更しない。
- **`backend/` は一切変更しない。** この issue はフロントに閉じる。
- **文字列はダブルクォート。** コメントは日本語。
- **`dark:` を新しく書かない**（7-2 の決定。ダークモードは対応範囲外）。
- **色は `globals.css` のトークンだけを使う。** `bg-canvas` / `bg-surface` / `text-ink` / `text-ink-muted` / `border-line` / `bg-accent` / `text-accent` / `text-danger` / `rounded-card`。`zinc-500` のような生のパレットを書かない。**新しい色トークンは足さない**（成功アイコンには既存の `text-accent` を流用する）。
- **`dangerouslySetInnerHTML` は eslint（`react/no-danger`）で禁止。**
- **`useSearchParams()` を使わない**（7-2 の決定。`<Suspense>` 境界が必要になり、ローカルでは通るのに Vercel の本番ビルドだけ落ちる）。
- **コンポーネントテストは書かない**（CLAUDE.md のテスト方針。React Testing Library は MVP では導入しない）。テストは `test/` に置き、`src/` の構造を写す。
- **`main` へ直接コミットしない。** 作業は `feat/toast-flash-message` 上で行う。
- コマンドはすべて `frontend/` ディレクトリで実行する。ローカルに Node が無い場合は `docker compose exec frontend <コマンド>` に読み替える。

## File Structure

| ファイル | 責務 |
|---|---|
| `frontend/src/lib/toast/toast-store.ts`（新規） | トーストの一覧を持ち、追加・削除・上限での押し出し・自動消去・購読者への通知を担う。React も DOM も知らない |
| `frontend/src/components/ui/toast-viewport.tsx`（新規） | ストアを購読して描画するだけの部品。ロジックを持たない |
| `frontend/test/lib/toast/toast-store.test.ts`（新規） | ストアの Vitest。node 環境・フェイクタイマー |
| `frontend/src/app/layout.tsx`（変更） | `<ToastViewport />` を 1 つ置く |
| `frontend/src/app/globals.css`（変更） | 入場アニメーションのキーフレームと reduced-motion |
| `frontend/src/components/dev/health-panel.tsx`（変更） | 7-3 が来るまでの唯一の呼び出し元。ローカルとプレビューでのみ描画される |
| `docs/issues_backlog.md`（変更） | 7-2.5 のタスクのチェックを埋める |

ロジックを全部ストア側に寄せ、ビューポートを「配列を並べるだけ」にするのが要点。こうすると機能全体がレンダリング抜きでテストでき、書かないと決めているコンポーネントテストが要らなくなる。

---

### Task 1: トーストのストア（キューと購読）

自動消去のタイマーはこのタスクでは作らない。Task 2 で足す。

**Files:**
- Create: `frontend/src/lib/toast/toast-store.ts`
- Test: `frontend/test/lib/toast/toast-store.test.ts`

**Interfaces:**
- Consumes: なし（最初のタスク）
- Produces:
  - `export type ToastType = "success" | "error"`
  - `export type Toast = { id: number; type: ToastType; message: string }`
  - `export const toast: { success(message: string): void; error(message: string): void; dismiss(id: number): void }`
  - `export const toastStore: { get(): readonly Toast[]; subscribe(listener: () => void): () => void; getServerSnapshot(): readonly Toast[] }`

- [ ] **Step 1: 失敗するテストを書く**

`frontend/test/lib/toast/toast-store.test.ts` を新規作成する。

```ts
// @vitest-environment node
//
// ストアは DOM を触らないので jsdom は要らない（vitest.config.mts のコメントが
// 定めている切り替え方に沿う）。
import { beforeEach, describe, expect, it, vi } from "vitest";

import { toast, toastStore } from "@/lib/toast/toast-store";

describe("toastStore", () => {
  beforeEach(() => {
    // モジュールスコープの状態はテスト間で持ち越される。テスト専用の export は
    // 足さず、公開 API だけで空に戻す。
    for (const item of toastStore.get()) toast.dismiss(item.id);
  });

  it("success で追加したトーストに type が付く", () => {
    toast.success("下書きを保存しました");

    expect(toastStore.get()).toEqual([
      { id: expect.any(Number), type: "success", message: "下書きを保存しました" },
    ]);
  });

  it("error で追加したトーストに type が付く", () => {
    toast.error("画像の生成に失敗しました");

    expect(toastStore.get()).toEqual([
      { id: expect.any(Number), type: "error", message: "画像の生成に失敗しました" },
    ]);
  });

  it("4 件目が来ると一番古いものを押し出し、残りは投入順で並ぶ", () => {
    // 投入順と五十音順をわざと逆相関させる。一致する文言（あ・い・う・え）を
    // 使うと、実装がうっかりソートしていてもこのテストが通ってしまう。
    toast.success("ぬま");
    toast.success("たき");
    toast.success("そら");
    toast.success("かぜ");

    expect(toastStore.get().map((item) => item.message)).toEqual(["たき", "そら", "かぜ"]);
  });

  it("dismiss は指定した 1 件だけ消す", () => {
    toast.success("ぬま");
    toast.success("たき");
    const second = toastStore.get()[1];

    toast.dismiss(second.id);

    expect(toastStore.get().map((item) => item.message)).toEqual(["ぬま"]);
  });

  it("存在しない id を dismiss しても配列の参照を変えない", () => {
    toast.success("ぬま");
    const before = toastStore.get();

    toast.dismiss(-1);

    expect(toastStore.get()).toBe(before);
  });

  it("変化が無ければ get は同じ参照を返す", () => {
    // useSyncExternalStore は戻り値を Object.is で比較する。毎回新しい配列を
    // 返すと「変わっていないのに変わった」と判定され、無限再レンダリングになる。
    toast.success("ぬま");

    expect(toastStore.get()).toBe(toastStore.get());
  });

  it("getServerSnapshot は毎回同じ参照の空配列を返す", () => {
    expect(toastStore.getServerSnapshot()).toEqual([]);
    expect(toastStore.getServerSnapshot()).toBe(toastStore.getServerSnapshot());
  });

  it("追加と削除で購読者に通知し、解除後は通知しない", () => {
    const listener = vi.fn();
    const unsubscribe = toastStore.subscribe(listener);

    toast.success("ぬま");
    toast.dismiss(toastStore.get()[0].id);

    expect(listener).toHaveBeenCalledTimes(2);

    unsubscribe();
    toast.success("たき");

    // 解除後に増えていないこと（2 のまま）。
    expect(listener).toHaveBeenCalledTimes(2);
  });
});
```

- [ ] **Step 2: テストを実行して失敗することを確かめる**

Run: `npm run test -- toast-store`

Expected: FAIL。`Failed to resolve import "@/lib/toast/toast-store"`（モジュールがまだ無い）。

- [ ] **Step 3: ストアを実装する**

`frontend/src/lib/toast/toast-store.ts` を新規作成する。

```ts
/**
 * トーストの置き場所。キューの管理だけを担う。React も DOM も知らない。
 *
 * tokenStore（src/lib/auth/token-store.ts）と同じ形にしてある。React の外に
 * 置くのは 2 つの理由による。(1) 7-2.6 で api.ts（React ではない）から通知を
 * 出す選択肢を塞がないため。(2) キュー管理を純粋ロジックとして、レンダリング
 * 抜きに Vitest で検証できるようにするため。
 */

export type ToastType = "success" | "error";

export type Toast = {
  id: number;
  type: ToastType;
  message: string;
};

/**
 * 同時に表示する最大件数。4 件目が来たら一番古いものを押し出す。
 *
 * 同じ文言の連打を 1 件に統合する方式は採らない。aria-live は「DOM のテキストが
 * 変化したこと」で読み上げるため、統合して置き換えると 2 回目以降が無音になる
 * （画面には見えているのに読み上げ環境には存在しない、という状態になる）。
 */
const MAX_VISIBLE = 3;

/**
 * 空配列は必ずこの 1 つを使い回す。useSyncExternalStore は戻り値を Object.is で
 * 比較するので、[] リテラルを毎回作ると無限再レンダリングになる。
 */
const EMPTY: readonly Toast[] = [];

let toasts: readonly Toast[] = EMPTY;
let nextId = 0;

const listeners = new Set<() => void>();

function notify(): void {
  for (const listener of listeners) listener();
}

function push(type: ToastType, message: string): void {
  const id = ++nextId;
  const next = [...toasts, { id, type, message }];

  // 上限を超えた分は古いほうから捨てる。
  while (next.length > MAX_VISIBLE) next.shift();

  toasts = next;
  notify();
}

function dismiss(id: number): void {
  const next = toasts.filter((item) => item.id !== id);

  // 無かった id なら参照を変えない。変えると購読側が無駄に再レンダリングする。
  if (next.length === toasts.length) return;

  toasts = next.length === 0 ? EMPTY : next;
  notify();
}

/**
 * 呼ぶ側の口。イベントハンドラや effect から使う。
 *
 * ここに出すのは**画面遷移を伴わない操作の結果**だけ。フォームの
 * バリデーションエラーは出さないこと（数秒で消えると、入力を直すために
 * 読む必要があるものが読めなくなる）。そちらは TextField の error prop が担当。
 */
export const toast = {
  success(message: string): void {
    push("success", message);
  },

  error(message: string): void {
    push("error", message);
  },

  dismiss(id: number): void {
    dismiss(id);
  },
};

/**
 * 描く側の口。ToastViewport だけが使う。
 *
 * tokenStore と同じく、どのメソッドも `this` を使わないこと。呼び出し側は
 * `useSyncExternalStore(toastStore.subscribe, toastStore.get, ...)` のように
 * メソッドを関数として切り離して渡すため、`this` を書いた瞬間に壊れる。
 */
export const toastStore = {
  /** 変化が無ければ同じ参照を返す（上の EMPTY のコメントを参照）。 */
  get(): readonly Toast[] {
    return toasts;
  },

  subscribe(listener: () => void): () => void {
    listeners.add(listener);
    return () => {
      listeners.delete(listener);
    };
  },

  /** SSR 時のスナップショット。サーバーにトーストは存在しない。 */
  getServerSnapshot(): readonly Toast[] {
    return EMPTY;
  },
};
```

- [ ] **Step 4: テストを実行して通ることを確かめる**

Run: `npm run test -- toast-store`

Expected: PASS（8 件）。

- [ ] **Step 5: lint と型検査を通す**

Run: `npm run lint`

Expected: エラーなし。

- [ ] **Step 6: コミット**

```bash
git add frontend/src/lib/toast/toast-store.ts frontend/test/lib/toast/toast-store.test.ts
git commit -m "$(cat <<'EOF'
feat: トーストのストア（キューと購読）

React を知らないモジュールスコープのストア。tokenStore と同型で、
useSyncExternalStore から購読できる形にした。

get() が変化の無いときに同じ参照を返すことをテストで固定している。
毎回新しい配列を返すと Object.is の比較が通らず無限再レンダリングになる。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_015CJXcJcVrfWnYh2DHQCRvc
EOF
)"
```

---

### Task 2: 自動消去のタイマー

タイマーをビューポート側の `useEffect` ではなくストアに置く。こうするとこの機能のロジックが 1 ファイルに閉じ、`vi.useFakeTimers()` だけでレンダリング抜きに全部テストできる。

**Files:**
- Modify: `frontend/src/lib/toast/toast-store.ts`
- Test: `frontend/test/lib/toast/toast-store.test.ts`（追記）

**Interfaces:**
- Consumes: Task 1 の `toast` / `toastStore` / `Toast` / `ToastType`
- Produces: 公開 API は変わらない。`toast.success()` / `toast.error()` が自動消去のタイマーを張るようになる（success 4000ms / error 8000ms）

- [ ] **Step 1: 失敗するテストを書く**

`frontend/test/lib/toast/toast-store.test.ts` の `beforeEach` をフェイクタイマー対応にし、`afterEach` を足す。

`import { beforeEach, describe, expect, it, vi } from "vitest";` を次に差し替える：

```ts
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";
```

`beforeEach` を次に差し替える：

```ts
  beforeEach(() => {
    // 片付けを先に済ませてからフェイクタイマーに切り替える。逆にすると、
    // 前のテストが張った実タイマーのハンドルをフェイク側で消すことになる。
    for (const item of toastStore.get()) toast.dismiss(item.id);
    vi.useFakeTimers();
  });

  afterEach(() => {
    vi.useRealTimers();
  });
```

`describe` ブロックの末尾（最後の `it` の後ろ、閉じ `});` の前）に次の 4 件を足す：

```ts
  it("success は 4 秒で自動的に消える", () => {
    toast.success("ぬま");

    vi.advanceTimersByTime(3999);
    expect(toastStore.get()).toHaveLength(1);

    vi.advanceTimersByTime(1);
    expect(toastStore.get()).toHaveLength(0);
  });

  it("error は 8 秒表示される（4 秒では消えない）", () => {
    // 失敗は読んで判断する必要がある（もう一度押すか決める）ので長くしてある。
    toast.error("画像の生成に失敗しました");

    vi.advanceTimersByTime(4000);
    expect(toastStore.get()).toHaveLength(1);

    vi.advanceTimersByTime(4000);
    expect(toastStore.get()).toHaveLength(0);
  });

  it("押し出されたトーストのタイマーを止める", () => {
    // 配列の中身だけを見るテストではこれを検知できない。止め忘れたタイマーが
    // 発火しても、その id は既に配列に無いので dismiss は何もせず、見た目は
    // 正しいままになる。動いているタイマーの本数を直接見る。
    toast.success("ぬま");
    toast.success("たき");
    toast.success("そら");
    expect(vi.getTimerCount()).toBe(3);

    toast.success("かぜ"); // ここで「ぬま」が押し出される

    expect(vi.getTimerCount()).toBe(3);
  });

  it("手動で閉じたトーストのタイマーを止める", () => {
    toast.success("ぬま");
    toast.success("たき");
    expect(vi.getTimerCount()).toBe(2);

    toast.dismiss(toastStore.get()[0].id);

    expect(vi.getTimerCount()).toBe(1);
  });
```

- [ ] **Step 2: テストを実行して失敗することを確かめる**

Run: `npm run test -- toast-store`

Expected: FAIL。新しい 4 件が落ちる（`expected 1 to be 0`、`expected 0 to be 3` など）。既存の 8 件は通ったまま。

- [ ] **Step 3: タイマーを実装する**

`frontend/src/lib/toast/toast-store.ts` を次のように変える。

`MAX_VISIBLE` の定義の直後に `DURATION_MS` を足す：

```ts
/**
 * 自動消去までの時間。error を長くするのは、失敗は読んで判断する必要がある
 * （「生成に失敗しました」→ もう一度押すか決める）のに対し、成功は認識する
 * だけで済むため。同じ 4 秒だと失敗の文面を読み切る前に消える。
 */
const DURATION_MS: Record<ToastType, number> = {
  success: 4000,
  error: 8000,
};
```

`const listeners = new Set<() => void>();` の直後にタイマーの置き場を足す：

```ts
/** 自動消去のタイマー。トーストが消える経路すべてで clearTimeout すること。 */
const timers = new Map<number, ReturnType<typeof setTimeout>>();

function clearTimer(id: number): void {
  const timer = timers.get(id);
  if (timer === undefined) return;
  clearTimeout(timer);
  timers.delete(id);
}
```

`push` を次に差し替える：

```ts
function push(type: ToastType, message: string): void {
  const id = ++nextId;
  const next = [...toasts, { id, type, message }];

  // 上限を超えた分は古いほうから捨てる。捨てるときにタイマーも止める。
  // 止め忘れると、既に画面から消えた通知のタイマーが数秒後に発火する。
  // その時点では id が配列に無いので見た目には何も起きず、テストでも
  // 配列を見ているだけでは気づけない（だからタイマーの本数を検査している）。
  while (next.length > MAX_VISIBLE) {
    const dropped = next.shift();
    if (dropped !== undefined) clearTimer(dropped.id);
  }

  toasts = next;
  timers.set(
    id,
    setTimeout(() => dismiss(id), DURATION_MS[type]),
  );
  notify();
}
```

`dismiss` の先頭に `clearTimer(id);` を足す：

```ts
function dismiss(id: number): void {
  clearTimer(id);

  const next = toasts.filter((item) => item.id !== id);

  // 無かった id なら参照を変えない。変えると購読側が無駄に再レンダリングする。
  if (next.length === toasts.length) return;

  toasts = next.length === 0 ? EMPTY : next;
  notify();
}
```

- [ ] **Step 4: テストを実行して通ることを確かめる**

Run: `npm run test -- toast-store`

Expected: PASS（12 件）。

- [ ] **Step 5: lint を通す**

Run: `npm run lint`

Expected: エラーなし。

- [ ] **Step 6: コミット**

```bash
git add frontend/src/lib/toast/toast-store.ts frontend/test/lib/toast/toast-store.test.ts
git commit -m "$(cat <<'EOF'
feat: トーストの自動消去（success 4 秒 / error 8 秒）

タイマーはビューポートではなくストアに置く。ロジックが 1 ファイルに閉じ、
フェイクタイマーだけでレンダリング抜きに検証できる。

押し出しと手動クローズの両方で clearTimeout することを、配列ではなく
動いているタイマーの本数で検査している。止め忘れても配列の中身は
正しいままなので、そちらを見ているだけでは検知できない。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_015CJXcJcVrfWnYh2DHQCRvc
EOF
)"
```

---

### Task 3: ビューポートとルートレイアウトへの設置

ここから先はコンポーネントテストを書かない（CLAUDE.md のテスト方針）。画面での確認は Task 4 で行う。

**Files:**
- Create: `frontend/src/components/ui/toast-viewport.tsx`
- Modify: `frontend/src/app/globals.css`（末尾に追記）
- Modify: `frontend/src/app/layout.tsx`

**Interfaces:**
- Consumes: Task 1・2 の `toast.dismiss(id)` / `toastStore.subscribe` / `toastStore.get` / `toastStore.getServerSnapshot` / 型 `ToastType`
- Produces: `export function ToastViewport(): JSX.Element`（引数なし。ルートレイアウトに 1 つだけ置く）

- [ ] **Step 1: アニメーションを `globals.css` に足す**

`frontend/src/app/globals.css` の末尾（`body { ... }` の後ろ）に追記する。

```css
/*
  トーストの入場アニメーション（7-2.5）。

  退場アニメーションは作らない。フェードさせるには「消える途中」という状態を
  ストアに足して実際の削除を遅らせる必要があり、ロジックをストアに閉じて
  テストできる形が、見た目のためだけの状態で崩れる。

  Tailwind のユーティリティではなく素の CSS で書くのは、キーフレームを
  伴うアニメーションが v4 のユーティリティだけでは表現できないため。
*/
@keyframes toast-in {
  from {
    opacity: 0;
    transform: translateY(0.5rem);
  }
  to {
    opacity: 1;
    transform: none;
  }
}

.animate-toast-in {
  animation: toast-in 150ms ease-out;
}

/* 動きを減らす設定をしているユーザーには動かさない。 */
@media (prefers-reduced-motion: reduce) {
  .animate-toast-in {
    animation: none;
  }
}
```

- [ ] **Step 2: ビューポートを実装する**

`frontend/src/components/ui/toast-viewport.tsx` を新規作成する。

```tsx
"use client";

import { useSyncExternalStore } from "react";

import { toast, toastStore, type ToastType } from "@/lib/toast/toast-store";

/**
 * 読み上げ用の種別の接頭辞。
 *
 * 視覚的にはアイコンの色と形が種別を伝えるが、読み上げ環境ではこれが唯一の
 * 手がかりになる。色だけで種別を区別しないという決定の、音声側の担保。
 */
const TYPE_LABEL: Record<ToastType, string> = {
  success: "成功：",
  error: "エラー：",
};

/** 種別のアイコン。読み上げは TYPE_LABEL が担うので aria-hidden でよい。 */
function ToastIcon({ type }: { type: ToastType }) {
  return (
    <svg
      viewBox="0 0 20 20"
      fill="currentColor"
      aria-hidden="true"
      className={`mt-0.5 size-5 shrink-0 ${type === "success" ? "text-accent" : "text-danger"}`}
    >
      {type === "success" ? (
        <path
          fillRule="evenodd"
          clipRule="evenodd"
          d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.7-9.3a1 1 0 00-1.4-1.4L9 10.6 7.7 9.3a1 1 0 00-1.4 1.4l2 2a1 1 0 001.4 0l4-4z"
        />
      ) : (
        <path
          fillRule="evenodd"
          clipRule="evenodd"
          d="M10 18a8 8 0 100-16 8 8 0 000 16zM9 6a1 1 0 112 0v5a1 1 0 11-2 0V6zm1 9.5a1.25 1.25 0 100-2.5 1.25 1.25 0 000 2.5z"
        />
      )}
    </svg>
  );
}

/**
 * トーストの表示先。ルートレイアウトに 1 つだけ置く。
 *
 * createPortal は使わない。ポータルが要るのは祖先に transform / filter /
 * contain があって position: fixed の基準がずれる場合だが、body にも
 * SiteHeader / SiteFooter にもそれらは無いので fixed がそのまま効く。
 * ポータルを足すと SSR とハイドレーションの整合を自前で面倒みることになる。
 */
export function ToastViewport() {
  const toasts = useSyncExternalStore(
    toastStore.subscribe,
    toastStore.get,
    toastStore.getServerSnapshot,
  );

  return (
    /*
      空のときも <ol> を描き続ける。スクリーンリーダーはライブリージョンを
      DOM に見つけた時点で監視を始めるため、リージョンと中身が同時に現れると
      読み上げを取りこぼす。

      aria-atomic="false" の明示が要点。role="status" の既定値は true で、
      そのままだと 2 件目が出たときに領域全体（＝ 1 件目も含めて）読み直される。

      pointer-events-none は、この領域が常時 DOM に居ることの裏返し。
      付けないと画面右下がずっとクリック不能になる。
    */
    <ol
      role="status"
      aria-live="polite"
      aria-atomic="false"
      className="pointer-events-none fixed inset-x-4 bottom-4 z-50 flex flex-col gap-2 sm:left-auto sm:right-4 sm:w-96"
    >
      {toasts.map((item) => (
        <li
          key={item.id}
          className="animate-toast-in pointer-events-auto flex items-start gap-2.5 rounded-card border border-line bg-surface p-3 shadow-md"
        >
          <ToastIcon type={item.type} />
          {/* 文言はフロントの辞書（固定文字列）。UGC は入らない */}
          <p className="flex-1 text-sm text-ink">
            <span className="sr-only">{TYPE_LABEL[item.type]}</span>
            {item.message}
          </p>
          <button
            type="button"
            onClick={() => toast.dismiss(item.id)}
            aria-label="通知を閉じる"
            className="-m-1 shrink-0 rounded-card p-1 text-ink-muted hover:text-ink"
          >
            <svg viewBox="0 0 20 20" fill="currentColor" aria-hidden="true" className="size-4">
              <path d="M6.3 5A.9.9 0 005 6.3L8.7 10 5 13.7A.9.9 0 006.3 15L10 11.3l3.7 3.7a.9.9 0 001.3-1.3L11.3 10 15 6.3A.9.9 0 0013.7 5L10 8.7 6.3 5z" />
            </svg>
          </button>
        </li>
      ))}
    </ol>
  );
}
```

- [ ] **Step 3: ルートレイアウトに置く**

`frontend/src/app/layout.tsx` を 2 箇所変える。

import を足す（`SiteHeader` の import の下、`AuthProvider` の import の上。アルファベット順の並びを保つ）：

```tsx
import { ToastViewport } from "@/components/ui/toast-viewport";
```

`<SiteFooter />` の直後に 1 行足す：

```tsx
          <SiteFooter />
          {/*
            トーストはここに 1 つだけ置く（7-2.5）。position: fixed なので
            DOM 上の位置は見た目に影響しないが、AuthProvider の内側に置いて
            おくと、将来トーストから認証状態を参照したくなったときに動かさずに済む。
          */}
          <ToastViewport />
```

- [ ] **Step 4: ビルドと lint を通す**

Run: `npm run lint && npx tsc --noEmit`

Expected: どちらもエラーなし。

- [ ] **Step 5: 既存のテストが壊れていないことを確かめる**

Run: `npm run test`

Expected: PASS（全ファイル）。

- [ ] **Step 6: コミット**

```bash
git add frontend/src/components/ui/toast-viewport.tsx frontend/src/app/globals.css frontend/src/app/layout.tsx
git commit -m "$(cat <<'EOF'
feat: トーストのビューポートをルートレイアウトに置く

ライブリージョンは空のときも DOM に描き続ける。リージョンと中身が同時に
現れるとスクリーンリーダーが読み上げを取りこぼすため。

role="status" の aria-atomic は既定で true なので false を明示する。
既定のままだと 2 件目が出たときに 1 件目まで読み直される。

常時 DOM に居る裏返しとして、コンテナに pointer-events-none を付ける。
付けないと画面右下がずっとクリック不能になる。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_015CJXcJcVrfWnYh2DHQCRvc
EOF
)"
```

---

### Task 4: 画面での確認手段と、ブラウザでの動作確認

7-3 が来るまで呼び出し元が存在しないため、「実装したが画面で一度も出していない」状態で PR を出すことになる。確認手段を用意する。

**Files:**
- Modify: `frontend/src/components/dev/health-panel.tsx`

**Interfaces:**
- Consumes: Task 1・2 の `toast.success(message)` / `toast.error(message)`、Task 3 の `ToastViewport`（ルートレイアウトに設置済み）
- Produces: なし（このタスクの成果物は画面での確認そのもの）

**なぜ `HealthPanel` に置くのか：** `src/app/page.tsx` の `SHOW_HEALTH_PANEL`（`NEXT_PUBLIC_VERCEL_ENV === "preview" || NODE_ENV !== "production"`）で出し分けられているため、**ローカルと Vercel プレビューにだけ出て本番には出ない**。7-1 の申し送りにある「本番トップに認証パネルが公開されていた」事故はこの出し分けを通さなかったことが原因なので、同じ轍を踏まない。新しいデバッグ用ページは作らない。

- [ ] **Step 1: 確認用ボタンを足す**

`frontend/src/components/dev/health-panel.tsx` を 2 箇所変える。

import を足す（`apiFetch` の import の下）：

```tsx
import { toast } from "@/lib/toast/toast-store";
```

`state.kind === "failure"` の `<p>` ブロックの直後、`</section>` の直前に足す：

```tsx
      {/*
        7-2.5 のトーストの確認用。最初の呼び出し元は 7-3（描写の「保存」）で、
        それまで画面からトーストを出す手段がここにしか無い。7-3 が入ったら
        このブロックは消してよい。

        page.tsx の SHOW_HEALTH_PANEL で出し分けられているので、ローカルと
        Vercel プレビューにだけ出て本番には出ない。
      */}
      <div className="mt-4 flex gap-2 border-t border-line pt-4">
        <button
          type="button"
          onClick={() => toast.success("下書きを保存しました")}
          className="rounded-card border border-line px-3 py-1.5 text-sm text-ink-muted hover:text-ink"
        >
          成功トースト
        </button>
        <button
          type="button"
          onClick={() => toast.error("画像の生成に失敗しました")}
          className="rounded-card border border-line px-3 py-1.5 text-sm text-ink-muted hover:text-ink"
        >
          エラートースト
        </button>
      </div>
```

- [ ] **Step 2: 開発環境を起動する**

Run: `docker compose up`（リポジトリのルートで）

Expected: `frontend` と `backend` が起動する。ブラウザで **`http://localhost:3001`** を開く（`docker-compose.yml` でフロントは `3001:3000`、Rails は `3000:3000`）。

トップページの下部に「Rails API 疎通確認」パネルと、その下に「成功トースト」「エラートースト」のボタンが 2 つ見えること。

> Turbopack の dev キャッシュで表示が古いままになることがある。ボタンが出てこない場合は `docs/` の切り分け手順（コミット `e3bd21d`）を参照。

- [ ] **Step 3: ブラウザで確認する**

以下を順に確認し、結果を記録する。

1. 「成功トースト」を押す → 画面右下に青緑のチェックアイコンと「下書きを保存しました」が出て、**約 4 秒で消える**
2. 「エラートースト」を押す → 赤の「!」アイコンと「画像の生成に失敗しました」が出て、**約 8 秒で消える**（4 秒では消えない）
3. 「成功トースト」を素早く 4 回押す → **同時に 3 件までしか出ない**。4 件目が出たとき一番上（最も古い）が消える
4. トーストの「×」を押す → その 1 件だけ消え、他は残る
5. **トーストが出ている間に、背後のリンク（ヘッダーの「Kotoe」など）が押せる**（`pointer-events` の確認）。トーストが出ていない状態でも、画面右下の何もない領域が普通にクリックできる
6. ブラウザの開発者ツールでウィンドウ幅を 400px にする → トーストが左右いっぱいに出て、**横スクロールバーが出ない**
7. **トーストがフォーカスを奪わないこと。**「成功トースト」を押した直後に開発者ツールのコンソールで `document.activeElement` を評価する → 押したボタンのままで、トーストの中の要素になっていない（7-3 で描写文を書いている最中にフォーカスが飛ぶと操作不能になるため）
8. コンソールにエラー・警告が出ていないこと（特に `useSyncExternalStore` 由来の無限ループ警告）

- [ ] **Step 4: 読み上げの確認（任意だができれば実施）**

macOS の VoiceOver（`Cmd + F5`）を有効にして「成功トースト」を押す。

Expected: 「成功：下書きを保存しました」と読み上げられる。2 回続けて押したとき、**1 件目が読み直されない**（`aria-atomic="false"` の効果）。

実施できない場合は、開発者ツールの Elements で `<ol role="status" aria-live="polite" aria-atomic="false">` が**トーストが 0 件のときも DOM に存在している**ことを目視で確認する。

- [ ] **Step 5: コミット**

```bash
git add frontend/src/components/dev/health-panel.tsx
git commit -m "$(cat <<'EOF'
chore: トーストの確認用ボタンを HealthPanel に足す

7-3 が来るまでトーストの呼び出し元が無く、画面で一度も出さないまま
PR を出すことになるため。

新しいデバッグ用ページは作らず HealthPanel に同居させる。page.tsx の
SHOW_HEALTH_PANEL で出し分けられており、ローカルと Vercel プレビューに
しか出ない。7-1 で暫定 UI を本番トップに公開した事故を繰り返さない。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_015CJXcJcVrfWnYh2DHQCRvc
EOF
)"
```

---

### Task 5: backlog の更新と PR

**Files:**
- Modify: `docs/issues_backlog.md:567-572`

**Interfaces:**
- Consumes: Task 1〜4 のすべて
- Produces: なし（最終タスク）

- [ ] **Step 1: backlog のチェックボックスを埋める**

`docs/issues_backlog.md` の 567〜572 行目、7-2.5 の「タスク：」配下の 5 項目を `- [ ]` から `- [x]` に変える。

変更前：

```markdown
  - [ ] トーストの表示コンポーネントと、呼び出し口（コンテキスト or フック）
  - [ ] 自動消去（数秒）と手動で閉じる手段
  - [ ] **スクリーンリーダーへの通知**（`role="status"` / `aria-live="polite"`）。視覚だけの通知は
        読み上げ環境では存在しないのと同じになる
  - [ ] 同じメッセージの連打を抑制するか決める（「保存」を連打したときの見え方）
  - [ ] 純粋なロジック（メッセージのキュー管理など）が切り出せたら Vitest（CLAUDE.md のテスト方針）
```

変更後：

```markdown
  - [x] トーストの表示コンポーネントと、呼び出し口（コンテキスト or フック）
  - [x] 自動消去（数秒）と手動で閉じる手段
  - [x] **スクリーンリーダーへの通知**（`role="status"` / `aria-live="polite"`）。視覚だけの通知は
        読み上げ環境では存在しないのと同じになる
  - [x] 同じメッセージの連打を抑制するか決める（「保存」を連打したときの見え方）
  - [x] 純粋なロジック（メッセージのキュー管理など）が切り出せたら Vitest（CLAUDE.md のテスト方針）
```

- [ ] **Step 2: 全テストと lint を最終確認する**

Run: `npm run test && npm run lint && npx tsc --noEmit`

Expected: すべて PASS / エラーなし。**出力を確認してから次に進むこと**（推測で「通ったはず」と書かない）。

- [ ] **Step 3: 本番ビルドが通ることを確かめる**

Run: `npm run build`

Expected: ビルド成功。`useSearchParams` 由来の `<Suspense>` エラーが出ないこと（このタスクでは使っていないが、Vercel だけで落ちる種類の失敗なのでローカルで一度踏む）。

- [ ] **Step 4: コミット**

```bash
git add docs/issues_backlog.md
git commit -m "$(cat <<'EOF'
docs: issue 7-2.5 のタスクを完了にする

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_015CJXcJcVrfWnYh2DHQCRvc
EOF
)"
```

- [ ] **Step 5: push 前に `/code-review` を通す**

Run: `/code-review`

レビュー結果を読み、指摘に対応する。**自己レビューで足りたと判断しない**（7-1 では自己レビュー 1 件に対して独立レビューが 9 件を挙げている）。

- [ ] **Step 6: push して PR を作る**

```bash
git push -u origin feat/toast-flash-message
```

PR 本文には次を必ず含める：

- `Closes #104`（issue テンプレートの要求。#16・#17 で 2 回連続で忘れて手動クローズになった前例がある）
- 依存を足さなかった理由（sonner 9.4 KB / react-hot-toast 5.2 KB。どちらも実行時に `<style>` を注入するため issue 8-5 の CSP で `style-src 'unsafe-inline'` が必要になる）
- 種別を success / error の 2 つにした理由
- 連打を抑制しなかった理由（`aria-live` が無音になる）
- Task 4 のブラウザ確認の結果
- 末尾は次の 2 行まで（**セッションリンクは公開リポジトリに載せない**）：

```
🤖 Generated with [Claude Code](https://claude.com/claude-code)
```

- [ ] **Step 7: Vercel プレビューで確認する**

PR に自動発行されるプレビュー URL を開き、Task 4 の Step 3 の 1〜6 をもう一度確認する。

プレビューでは `NEXT_PUBLIC_VERCEL_ENV === "preview"` なので `HealthPanel` が描画される。**本番（`kotoe.vercel.app`）には出ないこと**も、マージ後に必ず目視すること。

---

## 完了条件（設計書より）

- [ ] 画面遷移を伴わない操作の後に通知が出て、数秒で消える
- [ ] 手動で閉じられる
- [ ] スクリーンリーダーに読み上げられる（`role="status"` / `aria-live="polite"` / `aria-atomic="false"`、種別は `sr-only` の接頭辞で伝わる）
- [ ] 7-3 が `toast.success("下書きを保存しました")` の 1 行で使える
- [ ] `npm run test` と `npm run lint` が通る

## この計画で作らないもの

- 退場アニメーション、スワイプで消す、ホバーで一時停止、promise トースト、位置の切り替え、`info` 種別
- フォームのバリデーションエラーをトーストで出すこと（7-2 の `TextField` の `error` prop が担当）
- **7-2.6（セッション失効）そのもの。** `api.ts` から `toast.error()` を呼べる形にはなるが、この計画では呼ばない
- CSP の導入（issue 8-5）
