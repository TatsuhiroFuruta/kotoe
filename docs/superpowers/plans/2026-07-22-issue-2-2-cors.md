# issue 2-2 CORS 設定（JWT 対応）実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 別オリジン（Vercel）のフロントが Rails の発行した JWT を `Authorization` ヘッダから読み取れるようにし、本番ドメインと Vercel プレビューURLを環境変数だけで許可できる仕組みを作る。

**Architecture:** CORS の許可判定を `lib/cors/allowed_origins.rb` の PORO に切り出し、`config/initializers/cors.rb` は `origins` にブロックを渡してリクエストごとに委譲する。`rack-cors` は起動時に一度しか設定を読まないが、ブロックは毎リクエスト `call(source, env)` されるため、判定ロジックを単体テストでき、request spec からもスタブできる。許可オリジンは「完全一致リスト（`CORS_ALLOWED_ORIGINS`）」と「正規表現1本（`CORS_ALLOWED_ORIGIN_REGEX`）」の2本立て。

**Tech Stack:** Ruby on Rails 8.1（API モード）/ rack-cors 3.0.0 / devise-jwt / RSpec / rubocop-rails-omakase

**設計ドキュメント:** `docs/superpowers/specs/2026-07-21-issue-2-2-cors-design.md`
**ブランチ:** `feature/issue-2-2-cors`（作成済み。設計 doc はコミット済み）

## Global Constraints

- 文字列は**ダブルクォート**（spec を含むプロジェクト全体）。`.rubocop.yml` で `Style/StringLiterals: double_quotes` を強制している。
- コミット前に `bundle exec rubocop` と `bundle exec rspec` を通す。
- コマンドは**すべて Docker 経由**で実行する（`docker compose exec backend ...`）。ホストに Ruby/gem は入っていない。
- 作業ディレクトリはリポジトリルート `/Users/tatsuhirofuruta/workspace/projects/kotoe`。`docker compose` はここで実行する。Rails のパス（`lib/...`、`spec/...`）は `backend/` 配下を指す。
- ワイルドカード（`*`）の許可オリジンは使わない。`credentials: true` も設定しない（cookie は共有せず JWT を Authorization ヘッダで運ぶため）。
- `main` へ直接コミットしない。1 issue = 1 ブランチ = 1 PR。
- コミットメッセージ末尾に `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` を付ける。

## 実装済みの前提（変更しないこと）

- `rack-cors` の gem 追加と `CORS_ALLOWED_ORIGINS` の環境変数化は 0-3 で完了済み。
- 認証API（`POST /api/auth/sign_in` 等）と JWT 発行は 2-1 で完了済み。`sign_in` は成功時に `Authorization: Bearer xxx` を返す。
- `config.autoload_lib(ignore: %w[assets tasks])` が `backend/config/application.rb:29` に設定済み。したがって `lib/cors/allowed_origins.rb` は `Cors::AllowedOrigins` として autoload される。
- spec ヘルパ：`spec/support/auth_helpers.rb` が `sign_in_and_get_token` / `auth_headers` を `type: :request` に include 済み。FactoryBot は `create` で直接呼べる。

## 検証済みの技術的事実（実機で確認済み・前提にしてよい）

1. `rack-cors` 3.0.0 の `origins` はブロックを受け取り、`origin.call(source, env)` として**リクエストごとに**評価する（`resources.rb` の `allow_origin?`）。ブロックの引数は 2 つ。
2. `config/initializers/cors.rb` の**本体から** `Cors::AllowedOrigins` を参照すると `uninitialized constant Cors (NameError)` で boot が失敗する。initializer 実行時点では Zeitwerk の autoloader が未設定のため。
3. `Rails.application.config.after_initialize` のブロック内なら autoload が効き、そこで例外を出すと boot が中断する（＝ fail-fast が機能する）。
4. middleware に渡したブロック内での `Cors::AllowedOrigins` の定数解決は**リクエスト時**に行われるため問題なく動き、`allow(Cors::AllowedOrigins).to receive(:current)` によるスタブも example ごとに効く。
5. request spec で preflight を送るには `process :options, path, headers: {...}` を使う（`options` という helper は無い）。`rack-cors` の preflight レスポンスは **200**（204 ではない）。許可・不許可のどちらでも 200 が返り、差が出るのは `Access-Control-Allow-Origin` の有無。
6. `expose: ["Authorization"]` を設定すると、レスポンスに `Access-Control-Expose-Headers: Authorization` が付く。`Access-Control-Allow-Methods` は `"GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD"` の形で返る。

## ファイル構成

| ファイル | 区分 | 責務 |
|---|---|---|
| `backend/lib/cors/allowed_origins.rb` | 新規 | 許可オリジンの判定のみ。ENV の読み取りは `from_env` に閉じる。 |
| `backend/spec/lib/cors/allowed_origins_spec.rb` | 新規 | 判定ロジックの網羅テスト。ENV に触れず引数で組み立てる。 |
| `backend/config/initializers/cors.rb` | 書き換え | middleware への配線のみ。判定ロジックを持たない。 |
| `backend/spec/requests/cors_spec.rb` | 書き換え | 実際の HTTP レスポンスヘッダの検証。`skip` を外す。 |
| `.env.example` | 追記 | `CORS_ALLOWED_ORIGIN_REGEX` のプレースホルダとコメント。 |
| `CLAUDE.md` | 追記 | テスト規約の spec 置き場所に `spec/lib` を追加。 |

---

### Task 1: Cors::AllowedOrigins（判定ロジック）

**Files:**
- Create: `backend/lib/cors/allowed_origins.rb`
- Test: `backend/spec/lib/cors/allowed_origins_spec.rb`

**Interfaces:**
- Consumes: なし（この Task が最初）
- Produces: 後続 Task が依存する公開インターフェース
  - `Cors::AllowedOrigins.new(origins:, pattern: nil)` → インスタンス。`origins` は `Array<String>`、`pattern` は `String | nil`
  - `Cors::AllowedOrigins#allow?(origin)` → `true` / `false`。`origin` は `String | nil`
  - `Cors::AllowedOrigins.from_env(env = ENV)` → インスタンス
  - `Cors::AllowedOrigins.current` → インスタンス（メモ化）
  - 不正な正規表現を渡すと `initialize` が `RegexpError` を投げる

- [ ] **Step 1: 失敗するテストを書く**

`backend/spec/lib/cors/allowed_origins_spec.rb` を新規作成する。

`spec/lib` は `infer_spec_type_from_file_location!` の推測対象外なので type は付かない。PORO のテストなので type は不要。ただし autoload を効かせるため `rails_helper` を require する。

```ruby
require "rails_helper"

RSpec.describe Cors::AllowedOrigins do
  describe "#allow?" do
    context "完全一致リストのみ" do
      subject(:allowed) do
        described_class.new(origins: [ "http://localhost:3001", "https://kotoe.example.com" ])
      end

      it "リストに含まれるオリジンを許可する" do
        expect(allowed.allow?("http://localhost:3001")).to be true
        expect(allowed.allow?("https://kotoe.example.com")).to be true
      end

      it "リストにないオリジンを拒否する" do
        expect(allowed.allow?("https://evil.example.com")).to be false
      end

      it "スキームやポートが違えば拒否する" do
        expect(allowed.allow?("https://localhost:3001")).to be false
        expect(allowed.allow?("http://localhost:3000")).to be false
      end

      it "nil や空文字を拒否する" do
        expect(allowed.allow?(nil)).to be false
        expect(allowed.allow?("")).to be false
      end
    end

    context "設定値の正規化" do
      it "前後の空白と末尾のスラッシュを取り除いて比較する" do
        allowed = described_class.new(origins: [ "  https://kotoe.example.com/  " ])

        expect(allowed.allow?("https://kotoe.example.com")).to be true
      end

      it "空の要素を無視する" do
        allowed = described_class.new(origins: [ "", "  ", "https://kotoe.example.com" ])

        expect(allowed.allow?("")).to be false
        expect(allowed.allow?("https://kotoe.example.com")).to be true
      end
    end

    context "正規表現つき" do
      subject(:allowed) do
        described_class.new(
          origins: [ "https://kotoe.example.com" ],
          pattern: "https://kotoe-[a-z0-9-]+-kotoe-team\\.vercel\\.app"
        )
      end

      it "パターンに一致するプレビューURLを許可する" do
        expect(allowed.allow?("https://kotoe-git-feature-x-kotoe-team.vercel.app")).to be true
      end

      it "完全一致リストの側も引き続き許可する" do
        expect(allowed.allow?("https://kotoe.example.com")).to be true
      end

      it "チーム slug が違うプロジェクトを拒否する" do
        expect(allowed.allow?("https://kotoe-evil.vercel.app")).to be false
        expect(allowed.allow?("https://kotoe-evil-other-team.vercel.app")).to be false
      end
    end

    context "正規表現のアンカー" do
      # \A \z は実装側で付けるため、設定値に書かなくても部分一致で通り抜けない。
      subject(:allowed) do
        described_class.new(origins: [], pattern: "https://kotoe\\.example\\.com")
      end

      it "末尾に文字列を足したオリジンを拒否する" do
        expect(allowed.allow?("https://kotoe.example.com.evil.test")).to be false
      end

      it "先頭に文字列を足したオリジンを拒否する" do
        expect(allowed.allow?("https://evil.test/https://kotoe.example.com")).to be false
      end

      it "改行を挟んだオリジンを拒否する" do
        expect(allowed.allow?("https://evil.test\nhttps://kotoe.example.com")).to be false
      end

      it "ちょうど一致するオリジンは許可する" do
        expect(allowed.allow?("https://kotoe.example.com")).to be true
      end
    end

    context "pattern が未設定" do
      it "空文字なら完全一致のみで判定する" do
        allowed = described_class.new(origins: [ "https://kotoe.example.com" ], pattern: "")

        expect(allowed.allow?("https://kotoe.example.com")).to be true
        expect(allowed.allow?("https://kotoe-git-x-kotoe-team.vercel.app")).to be false
      end

      it "nil なら完全一致のみで判定する" do
        allowed = described_class.new(origins: [ "https://kotoe.example.com" ], pattern: nil)

        expect(allowed.allow?("https://kotoe-git-x-kotoe-team.vercel.app")).to be false
      end
    end

    context "設定が空" do
      it "何も許可しない" do
        allowed = described_class.new(origins: [], pattern: nil)

        expect(allowed.allow?("https://kotoe.example.com")).to be false
      end
    end
  end

  describe ".new" do
    it "不正な正規表現なら RegexpError を投げる" do
      expect {
        described_class.new(origins: [], pattern: "https://kotoe-[")
      }.to raise_error(RegexpError)
    end
  end

  describe ".from_env" do
    it "カンマ区切りのリストと正規表現を読み取る" do
      allowed = described_class.from_env(
        "CORS_ALLOWED_ORIGINS" => "http://localhost:3001, https://kotoe.example.com",
        "CORS_ALLOWED_ORIGIN_REGEX" => "https://kotoe-[a-z0-9-]+-kotoe-team\\.vercel\\.app"
      )

      expect(allowed.allow?("http://localhost:3001")).to be true
      expect(allowed.allow?("https://kotoe.example.com")).to be true
      expect(allowed.allow?("https://kotoe-git-x-kotoe-team.vercel.app")).to be true
      expect(allowed.allow?("https://evil.example.com")).to be false
    end

    it "環境変数が未設定なら何も許可しない" do
      allowed = described_class.from_env({})

      expect(allowed.allow?("http://localhost:3001")).to be false
    end
  end

  describe ".current" do
    it "ENV から組み立てたインスタンスを返す" do
      expect(described_class.current).to be_a(described_class)
    end

    it "同じインスタンスを返す（メモ化）" do
      expect(described_class.current).to equal(described_class.current)
    end
  end
end
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
docker compose exec backend bundle exec rspec spec/lib/cors/allowed_origins_spec.rb
```

Expected: FAIL。`uninitialized constant Cors`（`NameError`）でエラーになる。まだ実装が無いため。

- [ ] **Step 3: 実装を書く**

`backend/lib/cors/allowed_origins.rb` を新規作成する。

```ruby
module Cors
  # CORS で許可するオリジンの判定。
  #
  # 許可の指定は 2 通り：
  #   - CORS_ALLOWED_ORIGINS      … カンマ区切りの完全一致リスト（本番ドメイン、ローカル）
  #   - CORS_ALLOWED_ORIGIN_REGEX … 正規表現 1 本（Vercel のプレビューURLはデプロイごとに変わるため）
  #
  # config/initializers/cors.rb から 1 リクエストにつき 1 回呼ばれる。
  class AllowedOrigins
    ORIGINS_ENV_KEY = "CORS_ALLOWED_ORIGINS".freeze
    PATTERN_ENV_KEY = "CORS_ALLOWED_ORIGIN_REGEX".freeze

    class << self
      # ENV 由来のインスタンス。設定は起動後に変わらないのでメモ化する。
      def current
        @current ||= from_env
      end

      def from_env(env = ENV)
        new(
          origins: env.fetch(ORIGINS_ENV_KEY, "").split(","),
          pattern: env[PATTERN_ENV_KEY]
        )
      end
    end

    # @param origins [Array<String>] 完全一致で許可するオリジン
    # @param pattern [String, nil] 許可するオリジンの正規表現（\A \z は付けなくてよい）
    def initialize(origins:, pattern: nil)
      @origins = normalize(origins)
      @pattern = compile(pattern)
    end

    def allow?(origin)
      return false if origin.blank?

      @origins.include?(origin) || matches_pattern?(origin)
    end

    private

    def normalize(origins)
      origins.map { |origin| origin.to_s.strip.delete_suffix("/") }.reject(&:empty?)
    end

    # 前後を \A \z で囲んでからコンパイルする。
    # Ruby の ^ / $ は行頭・行末にマッチするため、設定値に書かせると
    # 改行を含むオリジンで意図しないマッチが起きうる。実装側で保証する。
    def compile(pattern)
      return nil if pattern.blank?

      Regexp.new("\\A#{pattern}\\z")
    end

    def matches_pattern?(origin)
      return false if @pattern.nil?

      @pattern.match?(origin)
    end
  end
end
```

- [ ] **Step 4: テストが通ることを確認する**

```bash
docker compose exec backend bundle exec rspec spec/lib/cors/allowed_origins_spec.rb
```

Expected: PASS（21 examples, 0 failures）。

- [ ] **Step 5: rubocop を通す**

```bash
docker compose exec backend bundle exec rubocop lib/cors/allowed_origins.rb spec/lib/cors/allowed_origins_spec.rb
```

Expected: `no offenses detected`。違反が出たら `bundle exec rubocop -a` で自動修正し、再度実行して確認する。

- [ ] **Step 6: コミット**

```bash
git add backend/lib/cors/allowed_origins.rb backend/spec/lib/cors/allowed_origins_spec.rb
git commit -m "$(cat <<'EOF'
feat: CORS の許可オリジン判定を Cors::AllowedOrigins に切り出す

完全一致リスト（CORS_ALLOWED_ORIGINS）と正規表現
（CORS_ALLOWED_ORIGIN_REGEX）の 2 本立てで判定する。
正規表現は \A \z を実装側で付け、設定者が書き忘れても
部分一致で通り抜けないようにする。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: initializer の配線と Authorization ヘッダの露出

**Files:**
- Modify: `backend/config/initializers/cors.rb`（全面書き換え）
- Test: `backend/spec/requests/cors_spec.rb`（全面書き換え）

**Interfaces:**
- Consumes: Task 1 の `Cors::AllowedOrigins.current` と `#allow?(origin)`
- Produces: HTTP レスポンスヘッダの挙動
  - 許可オリジンからのリクエストに `Access-Control-Allow-Origin: <origin>`
  - `Access-Control-Expose-Headers` に `Authorization` を含む
  - 不許可オリジンには `Access-Control-Allow-Origin` を返さない

- [ ] **Step 1: 失敗するテストを書く**

`backend/spec/requests/cors_spec.rb` の**中身を全部消して**以下に置き換える。

現行版は「ミドルウェアが起動時に一度だけ ENV を読む」ため ENV を差し替えられず `skip` していた。Task 1 で判定を PORO に出したので、`Cors::AllowedOrigins.current` をスタブすれば example ごとに設定を再現できる。

```ruby
require "rails_helper"

RSpec.describe "CORS", type: :request do
  let(:allowed_origin) { "https://kotoe.example.com" }
  let(:preview_origin) { "https://kotoe-git-feature-x-kotoe-team.vercel.app" }

  # 判定を PORO に切り出したので、ミドルウェアの起動時設定に触れずに
  # example ごとの設定を再現できる。
  before do
    allow(Cors::AllowedOrigins).to receive(:current).and_return(
      Cors::AllowedOrigins.new(
        origins: [ allowed_origin ],
        pattern: "https://kotoe-[a-z0-9-]+-kotoe-team\\.vercel\\.app"
      )
    )
  end

  describe "許可オリジン" do
    it "Access-Control-Allow-Origin を返す" do
      get "/api/health", headers: { "Origin" => allowed_origin }

      expect(response).to have_http_status(:ok)
      expect(response.headers["Access-Control-Allow-Origin"]).to eq(allowed_origin)
    end

    it "正規表現にマッチするプレビューURLも許可する" do
      get "/api/health", headers: { "Origin" => preview_origin }

      expect(response.headers["Access-Control-Allow-Origin"]).to eq(preview_origin)
    end
  end

  describe "不許可オリジン" do
    it "Access-Control-Allow-Origin を返さない" do
      get "/api/health", headers: { "Origin" => "https://evil.example.com" }

      expect(response.headers["Access-Control-Allow-Origin"]).to be_nil
    end

    it "チーム slug が違う Vercel プロジェクトを拒否する" do
      get "/api/health", headers: { "Origin" => "https://kotoe-evil.vercel.app" }

      expect(response.headers["Access-Control-Allow-Origin"]).to be_nil
    end
  end

  describe "preflight (OPTIONS)" do
    it "許可オリジンからの preflight に許可メソッドとヘッダを返す" do
      process :options, "/api/auth/sign_in", headers: {
        "Origin" => allowed_origin,
        "Access-Control-Request-Method" => "POST",
        "Access-Control-Request-Headers" => "Content-Type"
      }

      # rack-cors の preflight は 204 ではなく 200 を返す（実機で確認済み）。
      expect(response).to have_http_status(:ok)
      expect(response.headers["Access-Control-Allow-Origin"]).to eq(allowed_origin)
      expect(response.headers["Access-Control-Allow-Methods"]).to include("POST")
    end

    it "不許可オリジンからの preflight を許可しない" do
      process :options, "/api/auth/sign_in", headers: {
        "Origin" => "https://evil.example.com",
        "Access-Control-Request-Method" => "POST"
      }

      # 不許可でもステータスは 200 が返る。ブラウザは Access-Control-Allow-Origin が
      # 無いことで拒否するため、検証すべきはヘッダの不在。
      expect(response.headers["Access-Control-Allow-Origin"]).to be_nil
    end
  end

  # この issue の完了条件そのもの。
  # expose を設定しないと、別オリジンのフロントは JS から
  # Authorization ヘッダ（JWT）を読み取れない。
  describe "JWT の受け渡し" do
    let!(:user) { create(:user, email: "user@example.com", password: "password123") }

    it "許可オリジンからのログインで JWT を読み取れる" do
      post "/api/auth/sign_in",
        params: { user: { email: "user@example.com", password: "password123" } },
        headers: { "Origin" => allowed_origin },
        as: :json

      expect(response).to have_http_status(:ok)
      expect(response.headers["Access-Control-Allow-Origin"]).to eq(allowed_origin)
      expect(response.headers["Access-Control-Expose-Headers"]).to include("Authorization")
      expect(response.headers["Authorization"]).to match(/\ABearer .+\z/)
    end
  end
end
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
docker compose exec backend bundle exec rspec spec/requests/cors_spec.rb
```

Expected: FAIL。少なくとも「JWT の受け渡し」の example が
`expected "" to include "Authorization"`（`Access-Control-Expose-Headers` が空）で落ちる。
また現行の initializer は ENV 由来の固定リストを使うため、スタブが効かず許可オリジン系の example も落ちる。

- [ ] **Step 3: initializer を書き換える**

`backend/config/initializers/cors.rb` の**中身を全部消して**以下に置き換える。

```ruby
# フロント（ローカルは localhost:3001、本番は Vercel）は Rails と別オリジンのため、
# ブラウザからの API 呼び出しには CORS の許可が要る。
#
# 許可するオリジンの判定は lib/cors/allowed_origins.rb に置いてある。
# rack-cors は起動時に一度しか設定を読まないが、origins にブロックを渡すと
# リクエストごとに評価されるため、判定をテスト可能な形に保てる。
#
# 許可オリジンの実値（本番ドメイン・Vercel のチーム slug）は環境変数で渡す。
# ワイルドカード（"*"）は使わない。
Rails.application.config.middleware.insert_before 0, Rack::Cors do
  allow do
    origins { |source, _env| Cors::AllowedOrigins.current.allow?(source) }

    # expose がないと、別オリジンのフロントは JS から Authorization ヘッダを
    # 読み取れない（＝ 2-1 で発行した JWT を受け取れない）。
    #
    # cookie は共有せず JWT を Authorization ヘッダで運ぶ方針のため、
    # credentials: true は設定しない。
    resource "*",
      headers: :any,
      expose: [ "Authorization" ],
      methods: [ :get, :post, :put, :patch, :delete, :options, :head ]
  end
end

# 不正な正規表現を設定したまま本番へ出ると、全リクエストが CORS エラーになって
# 原因を追いにくい。起動時に一度評価して落とす。
#
# initializer の本体からは Cors::AllowedOrigins を参照できない。この時点では
# Zeitwerk の autoloader が未設定で uninitialized constant になるため、
# autoload が効く after_initialize で参照する。
Rails.application.config.after_initialize do
  Cors::AllowedOrigins.current
end
```

- [ ] **Step 4: テストが通ることを確認する**

```bash
docker compose exec backend bundle exec rspec spec/requests/cors_spec.rb
```

Expected: PASS（7 examples, 0 failures）。

- [ ] **Step 5: 全体のテストと rubocop を通す**

CORS はミドルウェアなので全リクエストに影響する。既存の request spec が壊れていないことを確認する。

```bash
docker compose exec backend bundle exec rspec
docker compose exec backend bundle exec rubocop
```

Expected: 両方とも 0 failures / `no offenses detected`。

- [ ] **Step 6: コミット**

```bash
git add backend/config/initializers/cors.rb backend/spec/requests/cors_spec.rb
git commit -m "$(cat <<'EOF'
feat: Authorization ヘッダを CORS で露出し、判定を PORO に委譲する

expose: ["Authorization"] がないと別オリジンのフロントは JWT を
読み取れない。あわせて origins にブロックを渡し、リクエストごとに
Cors::AllowedOrigins へ委譲する形にして spec の skip を外した。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: 環境変数のドキュメントと規約の更新

**Files:**
- Modify: `.env.example`（`CORS_ALLOWED_ORIGINS` の記述の直後に追記）
- Modify: `CLAUDE.md`（テスト規約の spec 置き場所の行）

**Interfaces:**
- Consumes: Task 1 の `CORS_ALLOWED_ORIGIN_REGEX` という環境変数名
- Produces: なし（ドキュメントのみ）

- [ ] **Step 1: .env.example に追記する**

`.env.example` の既存の記述はこうなっている。

```
# ブラウザから Rails API を叩けるオリジン（カンマ区切り）。フロントとバックは別オリジンのため必要。
# ローカルは Next.js の開発サーバー。本番（Render）には Vercel の URL を設定する。
CORS_ALLOWED_ORIGINS=http://localhost:3001
```

この `CORS_ALLOWED_ORIGINS=http://localhost:3001` の行の**直後**に、空行を 1 つ挟んで以下を追記する。

```
# Vercel のプレビューURLはデプロイごとに変わるため、完全一致リストではなく正規表現で許可する。
# 値は 8-2a（本番スケルトン）でチーム slug が確定してから Render の環境変数に設定する。
# ローカル開発では不要（未設定なら完全一致リストだけで判定する）。
#
# チーム slug まで含めること。"kotoe-" だけで絞ると、第三者が同じ接頭辞で
# Vercel プロジェクトを作るだけで許可されてしまう。
# 前後の \A \z は実装側（lib/cors/allowed_origins.rb）で付けるので書かなくてよい。
# CORS_ALLOWED_ORIGIN_REGEX=https://kotoe-[a-z0-9-]+-<your-team-slug>\.vercel\.app
```

実際のチーム slug は書かない（公開リポジトリのため）。行はコメントアウトしたままにする。

- [ ] **Step 2: CLAUDE.md のテスト規約を更新する**

`CLAUDE.md` の「Rails（backend/）」→「テストは RSpec」の中に以下の行がある。

```
  - 置き場所は `spec/models` / `spec/requests` / `spec/jobs`、ファクトリは `spec/factories`、共通ヘルパは `spec/support`（`rails_helper` が自動で読み込む）。
```

この行を以下に置き換える。

```
  - 置き場所は `spec/models` / `spec/requests` / `spec/jobs` / `spec/lib`（`lib/` 配下の PORO）、ファクトリは `spec/factories`、共通ヘルパは `spec/support`（`rails_helper` が自動で読み込む）。
```

- [ ] **Step 3: 変更内容を確認する**

```bash
git diff .env.example CLAUDE.md
```

Expected: `.env.example` に 7 行（コメント 6 行 + 空行）、`CLAUDE.md` に 1 行の置き換えが出ている。`.env.example` に実際のチーム slug やドメインが混入していないことを目視で確認する。

- [ ] **Step 4: コミット**

```bash
git add .env.example CLAUDE.md
git commit -m "$(cat <<'EOF'
docs: CORS_ALLOWED_ORIGIN_REGEX の設定例と spec/lib の規約を追記

正規表現はチーム slug まで含めること、実値の投入は 8-2a で行うことを
.env.example のコメントに残す。あわせて CLAUDE.md の spec 置き場所に
spec/lib を追加した。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: ローカルでのブラウザ疎通確認

設計ドキュメントの「完了条件の確認方法」の 2 番目にあたる。自動テストは HTTP ヘッダまでしか見ておらず、「ブラウザが実際に JS から読ませてくれるか」は CORS の本番挙動なので、一度だけ実機で確認する。

**Files:** なし（確認のみ。コードは変更しない）

**Interfaces:**
- Consumes: Task 2 で入れた `expose: ["Authorization"]`
- Produces: なし

- [ ] **Step 1: 開発環境を起動する**

```bash
docker compose up -d
docker compose ps
```

Expected: `backend` / `db` / `frontend` がすべて `running`。

- [ ] **Step 2: 確認用のユーザーを用意する**

```bash
docker compose exec backend bin/rails runner 'u = User.find_or_create_by!(email: "cors-check@example.com") { |x| x.name = "cors-check"; x.password = "password123" }; puts "READY: #{u.email}"'
```

Expected: `READY: cors-check@example.com`

- [ ] **Step 3: ブラウザから別オリジンで JWT を読めることを確認する**

ブラウザで `http://localhost:3001` を開き、DevTools のコンソールで以下を実行する。

`localhost:3001`（フロント）から `localhost:3000`（バック）を叩くので、これは実際に別オリジンのリクエストになる。

```js
const res = await fetch("http://localhost:3000/api/auth/sign_in", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({ user: { email: "cors-check@example.com", password: "password123" } }),
});
console.log("status:", res.status);
console.log("authorization:", res.headers.get("Authorization"));
```

Expected:
- `status: 200`
- `authorization: Bearer eyJ...`（`null` ではないこと）
- コンソールに CORS エラーが出ていないこと

`authorization: null` になる場合、`expose: ["Authorization"]` が効いていない。Task 2 Step 3 の initializer を見直し、`docker compose restart backend` で再起動してから再確認する（initializer の変更には再起動が要る）。

- [ ] **Step 4: 確認用ユーザーを片付ける**

```bash
docker compose exec backend bin/rails runner 'User.find_by(email: "cors-check@example.com")&.destroy!; puts "CLEANED"'
```

Expected: `CLEANED`

なお `User` には discard を入れていない（論理削除の対象は Post / Attempt）ため、ここは物理削除で問題ない。

- [ ] **Step 5: 作業ツリーが汚れていないことを確認する**

```bash
git status --porcelain
```

Expected: 何も出力されない（Task 3 までのコミットで完結しており、この Task はコードを変更しない）。

---

### Task 5: PR の作成

**Files:** なし

- [ ] **Step 1: 最終確認**

```bash
docker compose exec backend bundle exec rspec
docker compose exec backend bundle exec rubocop
```

Expected: 0 failures / `no offenses detected`。ここが通らないうちは PR を出さない。

- [ ] **Step 2: コミット内容を確認する**

```bash
git log --oneline main..HEAD
```

Expected: 5 コミット（設計 doc 2 件 + Task 1〜3 の 3 件）。

- [ ] **Step 3: push して PR を作る**

```bash
git push -u origin feature/issue-2-2-cors
gh pr create --base main --title "feat: CORS の Authorization 露出と許可オリジンの仕組み (issue 2-2)" --body "$(cat <<'EOF'
## 概要

issue 2-2。別オリジン（Vercel）のフロントが Rails の発行した JWT を読み取れるようにし、
本番ドメインと Vercel プレビューURLを環境変数だけで許可できるようにした。

Closes #10

## 変更点

- `expose: ["Authorization"]` を追加。これがないと別オリジンのフロントは JS から JWT を読めない。
- 許可判定を `lib/cors/allowed_origins.rb`（PORO）に切り出し、`origins` にブロックを渡して
  リクエストごとに委譲する形にした。`rack-cors` は起動時に一度しか設定を読まないため、
  従来の `spec/requests/cors_spec.rb` は ENV を差し替えられず `skip` していた。この `skip` を外した。
- 許可オリジンを「完全一致リスト（`CORS_ALLOWED_ORIGINS`）」＋「正規表現（`CORS_ALLOWED_ORIGIN_REGEX`）」の
  2 本立てにした。Vercel のプレビューURLはデプロイごとに変わるため、固定リストだけでは
  「プレビューURLで継続検証する」方針が回らない。

## セキュリティ上の判断

- 正規表現は `\A` `\z` を**実装側で**付ける。Ruby の `^` `$` は行頭・行末にマッチするため、
  設定値に書かせると改行を含むオリジンで抜けられる。spec で固定した。
- `.env.example` に「チーム slug まで含めること」を明記した。`https://kotoe-[a-z0-9-]+\.vercel\.app`
  のようなパターンだと、第三者が `kotoe-evil` という Vercel プロジェクトを作るだけで許可されてしまう。
- `credentials: true` は設定していない（cookie は共有せず JWT を Authorization ヘッダで運ぶため）。
- ワイルドカード（`*`）は使っていない。

## スコープ外

本番オリジンの**実値の投入**と本番URLでの疎通確認は 8-2a。
この設計では 8-2a で `backend/` のコードを変更する必要はなく、Render の環境変数を設定するだけで済む。

## 動作確認

- `bundle exec rspec` / `bundle exec rubocop` green
- ローカルのブラウザ（`localhost:3001` → `localhost:3000` の別オリジン）で
  `fetch` のレスポンスから `Authorization` ヘッダを読めることを確認済み

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 4: CI を確認する**

```bash
gh pr checks --watch
```

Expected: rubocop / rspec / brakeman / bundler-audit がすべて green。

---

## 自己レビュー結果

**1. 設計ドキュメントの網羅性**

| 設計の項目 | 対応する Task |
|---|---|
| `Cors::AllowedOrigins`（`current` / `from_env` / `initialize` / `allow?`） | Task 1 Step 3 |
| 置き場所を `lib/` に | Task 1 Step 3・Task 3 Step 2（規約更新） |
| `origins` の正規化（strip / 空要素除去 / 末尾スラッシュ） | Task 1（実装・テストとも） |
| `pattern` が nil / 空なら完全一致のみ | Task 1（実装・テストとも） |
| initializer の書き換え・`expose: ["Authorization"]` | Task 2 Step 3 |
| `credentials: true` を設定しない | Task 2 Step 3（コメントで明記） |
| アンカーの実装側強制 | Task 1 Step 3 の `compile` ＋「正規表現のアンカー」の 4 example |
| チーム slug を含める前提 | Task 1 の「チーム slug が違うプロジェクトを拒否する」＋ Task 3 Step 1 |
| 不正な正規表現で起動時に落とす | Task 2 Step 3 の `after_initialize` ＋ Task 1 の `RegexpError` example |
| model spec 8 ケース | Task 1 Step 1（設計の 8 項目をすべて含み、細分化して 21 examples） |
| request spec 4 ケース | Task 2 Step 1（設計の 4 項目 + 不許可 preflight + slug 偽装で 7 examples） |
| `.env.example` の追記 | Task 3 Step 1 |
| 完了条件の確認方法（rspec/rubocop・ブラウザ疎通） | Task 4・Task 5 Step 1 |

設計に対する未対応項目なし。

**2. プレースホルダの走査**

「TBD」「後で実装」「適切にエラー処理」「上記のテストを書く」に相当する記述なし。コードを変更する全ステップに実際のコードを載せた。`.env.example` の `<your-team-slug>` は意図的なプレースホルダ（実値は 8-2a で投入する設計）。

**3. 型・シグネチャの一貫性**

- `Cors::AllowedOrigins.new(origins:, pattern:)` — Task 1 の実装・テスト、Task 2 の request spec のスタブで一致。
- `Cors::AllowedOrigins.current` — Task 1 の定義、Task 2 の initializer と spec のスタブで一致。
- `#allow?(origin)` — Task 1 の定義、Task 2 の initializer の呼び出しで一致。
- 環境変数名 `CORS_ALLOWED_ORIGINS` / `CORS_ALLOWED_ORIGIN_REGEX` — Task 1 の定数、Task 3 の `.env.example` で一致。
- テスト用のチーム slug は Task 1・Task 2 とも `kotoe-team` で統一。
